name: Phase 14 superseded read-only probe (frozen)

on:
  workflow_dispatch:

permissions:
  contents: read

# Superseded after the actual deployed English fixture was identified as Y5E10.

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded Phase 14 read-only probe is frozen.'
