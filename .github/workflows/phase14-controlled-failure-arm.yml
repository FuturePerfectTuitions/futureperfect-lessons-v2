name: Phase 14 controlled failure workflow (frozen after PASS)

on:
  workflow_dispatch:

permissions:
  contents: read

# Historical Phase 14 TestY-only controlled Worker-unavailable fixture. No active failure hook remains deployed.

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Phase 14 controlled-failure workflow is permanently frozen after acceptance.'
