name: Phase 14 frozen temporary workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'FROZEN: Phase 14 temporary acceptance workflow. Do not use as current-state evidence unless explicitly re-enabled.'
