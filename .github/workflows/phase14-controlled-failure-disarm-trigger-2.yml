name: Phase 14 controlled failure secondary disarm workflow (frozen after PASS)

on:
  workflow_dispatch:

permissions:
  contents: read

# Historical no-op/freeze evidence only. No Phase 14 failure hook remains deployed.

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Phase 14 secondary disarm workflow is permanently frozen.'
