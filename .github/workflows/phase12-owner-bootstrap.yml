name: Phase 12 owner payload bootstrap

on:
  pull_request:
    branches: [main]
    paths:
      - .github/phase12-owner-bootstrap-trigger.txt
      - .github/workflows/phase12-owner-bootstrap.yml

permissions:
  contents: read

jobs:
  bootstrap:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Bootstrap frozen after secure key generation.'
