name: Incident - Read-only Semantic Entitlement Audit

on:
  push:
    branches:
      - incident/entitlement-audit-2026-09-15
    paths:
      - scripts/incident-entitlement-semantic-audit-v2.mjs
      - .github/workflows/incident-entitlement-semantic-audit.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: incident-entitlement-semantic-audit-2026-09-15
  cancel-in-progress: false

env:
  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  LEGACY_WORKER: fpt-portal-v2-worker
  AS_OF_DATE: '2026-09-15'
  TARGET_USER: dha2806
  TARGET_LESSON: Y4T1EE01

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Syntax and mutation guard
        shell: bash
        run: |
          set -Eeuo pipefail
          node --check scripts/incident-entitlement-semantic-audit-v2.mjs
          ! grep -Eiq "sql\([^,]+,[[:space:]]*['\"](INSERT|UPDATE|DELETE|DROP|ALTER|CREATE|REPLACE|VACUUM)" scripts/incident-entitlement-semantic-audit-v2.mjs

      - name: Existing consistency/parity audit control
        shell: bash
        env:
          WORKER_NAME: fpt-portal-v2-worker
          CHECKPOINT8_AS_OF_DATE: '2026-09-15'
        run: |
          set -u
          node scripts/rebuild-checkpoint8-live-audit.mjs | tee /tmp/existing-parity-audit.log
          code=${PIPESTATUS[0]}
          if [ -s /tmp/checkpoint8-live-summary.json ]; then cp /tmp/checkpoint8-live-summary.json /tmp/existing-parity-audit.json; fi
          echo "control_exit_code=$code"
          exit 0

      - name: Independent semantic all-user entitlement audit
        shell: bash
        run: |
          set -Eeuo pipefail
          set +e
          node scripts/incident-entitlement-semantic-audit-v2.mjs | tee /tmp/semantic-audit-console.log
          code=${PIPESTATUS[0]}
          set -e
          if [ "$code" -ne 0 ] && [ "$code" -ne 2 ]; then exit "$code"; fi
          test -s /tmp/entitlement-semantic-audit.json
          test -s /tmp/dha2806-trace.json
          echo "semantic_audit_exit_code=$code"

      - name: Upload safe read-only evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: entitlement-semantic-audit-2026-09-15
          path: |
            /tmp/existing-parity-audit.json
            /tmp/existing-parity-audit.log
            /tmp/entitlement-semantic-audit.json
            /tmp/dha2806-trace.json
            /tmp/semantic-audit-console.log
          if-no-files-found: error
          retention-days: 30
