name: Phase 14 desktop fixture setup (frozen after READY)

on:
  workflow_dispatch:

permissions:
  contents: read

# Desktop fixtures were prepared once with zero TestY entitlements.
# Frozen to prevent duplicate assignment creation while owner acceptance is pending.

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Phase 14 desktop fixtures are already READY; setup is frozen.'
