name: Phase 14 desktop fixture setup (frozen after PASS)

on:
  workflow_dispatch:

permissions:
  contents: read

# Phase 14 desktop fixtures were created once, accepted, and then removed by cleanup run 33072650628.
# Frozen permanently for the Phase 14 closure checkpoint.

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Phase 14 fixture setup is frozen after completed acceptance and cleanup.'
