name: Post-Step-10 MEA-07 family rules read-only

on:
  push:
    branches:
      - mastery/post-step10-expansion-planning-2026-09-18
    paths:
      - '.github/workflows/post-step10-mea07-family-rules-readonly.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  WRANGLER_VERSION: 4.134.0
  QUIZ_D1_NAME: fpt-11plus-practice
  QUIZ_D1_ID: 5b988d3d-341a-492d-8e40-8d4efa66150e

jobs:
  rules:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - shell: bash
        run: npm install --no-save --no-package-lock "wrangler@${WRANGLER_VERSION}"
      - name: Export MEA-07 family rules only
        shell: bash
        run: |
          set -Eeuo pipefail
          cat >/tmp/w.toml <<TOML
          name = "post-step10-mea07-family-rules-readonly"
          compatibility_date = "2026-09-18"
          [[d1_databases]]
          binding = "DB"
          database_name = "${QUIZ_D1_NAME}"
          database_id = "${QUIZ_D1_ID}"
          TOML
          npx wrangler d1 execute "$QUIZ_D1_NAME" --remote --config /tmp/w.toml --command "SELECT family_id,family_name,primary_skill_id,gl_method_signature,generator_rules_json,earliest_release_point,question_level_override_needed FROM question_family WHERE family_id='MEA-07';" --json >/tmp/rules.json
          python3 - <<'PY'
          import json
          from pathlib import Path
          rows=json.loads(Path('/tmp/rules.json').read_text())[0]['results']
          assert len(rows)==1
          Path('/tmp/mea07_family_rules.json').write_text(json.dumps(rows[0],indent=2),encoding='utf-8')
          print(json.dumps({'marker':'POST_STEP10_MEA07_FAMILY_RULES_READONLY_PASS','rows':1,'source_question_text_exported':False},indent=2))
          PY
          rm -f /tmp/rules.json
      - uses: actions/upload-artifact@v4
        with:
          name: post-step10-mea07-family-rules-readonly
          path: /tmp/mea07_family_rules.json
          retention-days: 30
