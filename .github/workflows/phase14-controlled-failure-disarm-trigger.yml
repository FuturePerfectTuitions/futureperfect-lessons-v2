name: Phase 14 controlled failure disarm workflow (frozen after PASS)

on:
  workflow_dispatch:

permissions:
  contents: read

# Historical workflow only. Canonical development Worker was restored before successful retry and remains deployed.

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Phase 14 controlled-failure disarm workflow is permanently frozen.'
