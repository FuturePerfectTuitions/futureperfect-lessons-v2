name: Phase 14 superseded acceptance workflow (frozen)

on:
  workflow_dispatch:

permissions:
  contents: read

# Superseded by the corrected final backend acceptance, which passed as run 33060941602.
# Frozen to prevent obsolete Y5E1 fixture assumptions from generating misleading PR failures.

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded Phase 14 workflow is frozen.'
