name: Phase 14 superseded acceptance workflow (frozen)

on:
  workflow_dispatch:

permissions:
  contents: read

# Superseded by the corrected final backend acceptance, which passed as run 33060941602.
# Retained only as historical evidence and permanently frozen for the Phase 14 checkpoint.

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded Phase 14 acceptance workflow is frozen.'
