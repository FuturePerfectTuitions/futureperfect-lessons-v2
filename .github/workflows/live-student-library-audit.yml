name: Temporary live full-library audit
on:
  push:
    branches: [ops/live-library-audit]
    paths: [.github/workflows/live-student-library-audit.yml]
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary audit placeholder"
