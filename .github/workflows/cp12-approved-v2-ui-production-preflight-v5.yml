name: CP12 approved V2 UI production preflight v5

on:
  push:
    branches: [fix/cp12-approved-v2-ui-staging-2026-09-14]
    paths: ['.github/workflows/cp12-approved-v2-ui-production-preflight-v5.yml']

permissions:
  contents: read

env:
  PROD_HOST: lessons.futureperfect.education
  LEGACY_WORKER: fpt-portal-v2-worker
  BROWSER_WORKER: fpt-portal-v2-rebuild-browser-prod
  STUDENT_WORKER: fpt-portal-v2-rebuild-student-prod
  EXPECTED_READ_MODELS_KV: 77b35165c8694087bc1b0515c35a7e89
  EXPECTED_STUDENTS_KV: c9723c8806334e4ea54d1b456d31b794
  EXPECTED_DB: 97250a54-fa91-45ad-a002-3c4566b1fc38
  EXPECTED_R2: fpt-materials-dev
  EXPECTED_STUDENT_VERSION: bed8e660-facb-47f3-afad-7a9996d95c5a
  EXPECTED_STUDENT_ROLLBACK: 0751feed-5aa3-40fb-854f-5847c8603872
  EXPECTED_BROWSER_VERSION: 412d5dd1-8f36-4e84-b5b6-eba39c670820
  EXPECTED_LEGACY_VERSION: b1c8326e-a9cc-4150-b43b-cc572a4404c1
  OFFICIAL_CP12_SHA: b5e38b3768d269f632bbf4fc95e7d3f2a3fb3ba2
  METADATA_COMMIT: 9793028e34b16972defa64d3e465db907868e645
  METADATA_HELPER_BLOB: 4cda45292706942a0e9f6b3c701a71c2a55c7d91
  UAT_BLOB: df0e5e9c1b5e2a8099985be98974c5b54f5e5b17
  PERSONA_PREREQ_BLOB: fae56dc89964fdaa8c6992fb2098b94012f9da7c
  PROMOTION_WORKFLOW_BLOB: 22da830eb1f84b1e8f9e8c06e5a675e865cdb35a
  CP12_METADATA_BACKUP: /tmp/cp12-v5-preview-backup.json
  CP12_METADATA_REPORT: /tmp/cp12-v5-preview-report.json

jobs:
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CP12_METADATA_ACTION: preview
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Freeze exact CP12 product and UAT sources
        shell: bash
        run: |
          set -Eeuo pipefail
          git fetch origin rebuild/checkpoint12-stabilisation-2026-09-14
          test "$(git rev-parse origin/rebuild/checkpoint12-stabilisation-2026-09-14)" = "$OFFICIAL_CP12_SHA"
          git merge-base --is-ancestor "$OFFICIAL_CP12_SHA" HEAD
          for path in rebuild/adminops/src/lib/compiler.mjs rebuild/shared/read-models/phase11-extension-resources.mjs rebuild/student/src/lib/runtime.mjs tests/rebuild-cp12-resource-presentation-groups.mjs; do
            test "$(git rev-parse "${METADATA_COMMIT}:${path}")" = "$(git hash-object "$path")"
          done
          test "$(git hash-object scripts/cp12-resource-ui-production-metadata.mjs)" = "$METADATA_HELPER_BLOB"
          test "$(git hash-object scripts/cp12-approved-v2-ui-production-uat.mjs)" = "$UAT_BLOB"
          test "$(git hash-object scripts/cp12-production-real-persona-prereq.mjs)" = "$PERSONA_PREREQ_BLOB"
          test "$(git hash-object .github/workflows/cp12-approved-v2-ui-production-promote.yml)" = "$PROMOTION_WORKFLOW_BLOB"
          node --check scripts/cp12-resource-ui-production-metadata.mjs
          node --check scripts/cp12-production-real-persona-prereq.mjs
          node --check scripts/cp12-approved-v2-ui-production-uat.mjs
          grep -F 'CP12_PRODUCTION_UAT_REAL_PERSONA_AUTODISCOVERY_PASS' scripts/cp12-approved-v2-ui-production-uat.mjs >/dev/null
          grep -F "username.toLowerCase()==='admin'" scripts/cp12-approved-v2-ui-production-uat.mjs >/dev/null
          node tests/rebuild-cp12-resource-presentation-groups.mjs
          node tests/rebuild-checkpoint8-phase11-resource-parity.mjs
          node tests/rebuild-checkpoint11-scope-compatibility.mjs
          node tests/rebuild-checkpoint11-vr-howto-runtime.mjs
          echo CP12_V5_EXACT_SOURCE_PASS

      - name: Fresh read-only production topology versions bindings and rollback gate
        shell: bash
        run: |
          set -Eeuo pipefail
          AUTH=(--header "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}")
          API="https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}"
          curl --fail --silent --show-error "https://api.cloudflare.com/client/v4/zones?name=futureperfect.education&account.id=${CLOUDFLARE_ACCOUNT_ID}" "${AUTH[@]}" -o /tmp/zones.json
          ZONE_ID="$(jq -r '.result[]|select(.name=="futureperfect.education")|.id' /tmp/zones.json | head -n1)"; test -n "$ZONE_ID"
          curl --fail --silent --show-error "https://api.cloudflare.com/client/v4/zones/${ZONE_ID}/workers/routes" "${AUTH[@]}" -o /tmp/routes.json
          jq -e --arg host "${PROD_HOST}/*" --arg browser "$BROWSER_WORKER" '.success==true and ([.result[]|select(.pattern==$host and .script==$browser)]|length)==1 and ([.result[]|select(.pattern==$host and .script!=$browser)]|length)==0' /tmp/routes.json >/dev/null
          for worker in "$LEGACY_WORKER" "$BROWSER_WORKER" "$STUDENT_WORKER"; do
            curl --fail --silent --show-error "${API}/workers/scripts/${worker}/settings" "${AUTH[@]}" -o "/tmp/${worker}-settings.json"
            curl --fail --silent --show-error "${API}/workers/scripts/${worker}/deployments" "${AUTH[@]}" -o "/tmp/${worker}-deployments.json"
          done
          test "$(jq -r '.result.deployments[0].versions[]|select((.percentage//0)==100)|.version_id' "/tmp/${STUDENT_WORKER}-deployments.json" | head -n1)" = "$EXPECTED_STUDENT_VERSION"
          test "$(jq -r '.result.deployments[0].versions[]|select((.percentage//0)==100)|.version_id' "/tmp/${BROWSER_WORKER}-deployments.json" | head -n1)" = "$EXPECTED_BROWSER_VERSION"
          test "$(jq -r '.result.deployments[0].versions[]|select((.percentage//0)==100)|.version_id' "/tmp/${LEGACY_WORKER}-deployments.json" | head -n1)" = "$EXPECTED_LEGACY_VERSION"
          jq -e --arg student "$STUDENT_WORKER" '([.result.bindings[]|select(.type=="service")]|length)==1 and any(.result.bindings[];.name=="STAGING_API" and .service==$student) and ([.result.bindings[]|select(.type=="service" and .name!="STAGING_API")]|length)==0' "/tmp/${BROWSER_WORKER}-settings.json" >/dev/null
          jq -e --arg id "$EXPECTED_READ_MODELS_KV" 'any(.result.bindings[];.name=="READ_MODELS_KV" and (.namespace_id//.id)==$id)' "/tmp/${STUDENT_WORKER}-settings.json" >/dev/null
          jq -e --arg id "$EXPECTED_STUDENTS_KV" 'any(.result.bindings[];.name=="STUDENTS_KV" and (.namespace_id//.id)==$id)' "/tmp/${STUDENT_WORKER}-settings.json" >/dev/null
          jq -e --arg id "$EXPECTED_DB" 'any(.result.bindings[];.name=="DB" and (.database_id//.id)==$id)' "/tmp/${STUDENT_WORKER}-settings.json" >/dev/null
          jq -e --arg bucket "$EXPECTED_R2" 'any(.result.bindings[];.name=="MATERIALS_R2" and (.bucket_name//.bucket)==$bucket)' "/tmp/${STUDENT_WORKER}-settings.json" >/dev/null
          curl --fail --silent --show-error "${API}/workers/scripts/${STUDENT_WORKER}/versions?deployable=true" "${AUTH[@]}" -o /tmp/student-versions.json
          curl --fail --silent --show-error "${API}/workers/scripts/${BROWSER_WORKER}/versions?deployable=true" "${AUTH[@]}" -o /tmp/browser-versions.json
          jq -e --arg v "$EXPECTED_STUDENT_ROLLBACK" 'any(.result.items[];.id==$v)' /tmp/student-versions.json >/dev/null
          jq -e --arg v "$EXPECTED_STUDENT_VERSION" 'any(.result.items[];.id==$v)' /tmp/student-versions.json >/dev/null
          jq -e --arg v "$EXPECTED_BROWSER_VERSION" 'any(.result.items[];.id==$v)' /tmp/browser-versions.json >/dev/null
          echo CP12_V5_PRODUCTION_BASELINE_PASS

      - name: Reprove real ordinary and 11 plus student UAT principals read-only
        shell: bash
        env:
          STUDENTS_KV_ID: ${{ env.EXPECTED_STUDENTS_KV }}
          CP12_PERSONA_SUMMARY: /tmp/cp12-v5-personas.json
          CP12_PERSONA_SECRETS: /tmp/cp12-v5-personas-secret.json
        run: |
          set -Eeuo pipefail
          node scripts/cp12-production-real-persona-prereq.mjs >/tmp/persona-prereq.log
          jq -e '.marker=="CP12_PRODUCTION_REAL_PERSONA_PREREQ_READONLY_PASS" and .status=="PASS" and .productionMutation==false and .ordinary.y5e2Open==true and .ordinary.year5ElevenPlusAbsent==true and .vr.y5e2Open==true and .ordinary.credentialDisclosed==false and .vr.credentialDisclosed==false and .secretMaterialLogged==false' "$CP12_PERSONA_SUMMARY" >/dev/null
          rm -f "$CP12_PERSONA_SECRETS" /tmp/persona-prereq.log
          echo CP12_V5_REAL_PERSONAS_READONLY_PASS

      - name: Recompute exact prepared metadata preview without writes
        shell: bash
        run: |
          set -Eeuo pipefail
          node scripts/cp12-resource-ui-production-metadata.mjs
          jq -e '.marker=="CP12_RESOURCE_UI_METADATA_PREVIEW_PASS" and .status=="PASS" and .writes==false and .publishedLessonScopes==372 and .changedLessonScopes==369 and .unchangedLessonScopes==3 and .totalResources==1509 and .nonPresentationGroupDiffs==0 and .coreResources==984 and .elevenPlusResources==212 and .vrResources==313' "$CP12_METADATA_REPORT" >/dev/null
          echo CP12_V5_METADATA_PREVIEW_PASS

      - name: Assemble final read-only preflight evidence
        shell: bash
        run: |
          set -Eeuo pipefail
          jq -n --arg marker CP12_APPROVED_V2_UI_PRODUCTION_PREFLIGHT_V5_PASS --arg head "$GITHUB_SHA" --arg official "$OFFICIAL_CP12_SHA" --arg uat "$UAT_BLOB" --arg persona "$PERSONA_PREREQ_BLOB" --slurpfile preview "$CP12_METADATA_REPORT" --slurpfile principals /tmp/cp12-v5-personas.json '{marker:$marker,status:"PASS",head:$head,officialCp12:$official,productionMutation:false,topologyFresh:true,rollbackRetained:true,realStudentUatReady:true,uatBlob:$uat,personaPrereqBlob:$persona,metadataPreview:$preview[0],principals:$principals[0]}' | tee /tmp/cp12-approved-v2-ui-production-preflight-v5.json

      - uses: actions/upload-artifact@v4
        with:
          name: cp12-approved-v2-ui-production-preflight-v5
          path: |
            /tmp/cp12-approved-v2-ui-production-preflight-v5.json
            /tmp/cp12-v5-preview-report.json
            /tmp/cp12-v5-personas.json
          if-no-files-found: error
          retention-days: 30
