name: Phase 14 final backend acceptance (frozen after PASS)

on:
  workflow_dispatch:

permissions:
  contents: read

# Phase 14 backend acceptance conclusively passed on run 33060941602.
# This workflow is intentionally frozen so later desktop-acceptance preparation
# cannot repeat already-passed database mutations merely because PR #124 changes.

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Phase 14 backend acceptance is frozen after conclusive PASS run 33060941602.'
