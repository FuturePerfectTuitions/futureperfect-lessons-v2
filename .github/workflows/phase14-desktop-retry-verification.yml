name: Phase 14 desktop retry verification (frozen after PASS)

on:
  workflow_dispatch:

permissions:
  contents: read

# Historical acceptance evidence only. Desktop retry and backend idempotency both passed.

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Phase 14 desktop retry verification is permanently frozen.'
