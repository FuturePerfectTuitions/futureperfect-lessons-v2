# Incident importer exact-version reactivation V2 trigger

The prior reactivation proved the target Worker version, topology, bindings, authentication boundary, and semantic entitlement state, then auto-rolled back only because Bash `ERR` trapping intercepted the roster audit's expected non-zero status before its JSON could be evaluated.

This trigger authorizes reactivation of only immutable Worker version `2eec1f4d-daec-4d40-be55-b779852ddfb2`. The V2 gate handles the known roster-audit non-zero status inside an `||` list, then evaluates the generated JSON. Critical entitlement/profile/source-batch defects must remain zero, the existing 21 D1 assignment discrepancies must remain byte-equivalent as normalized JSON, public Browser -> Student topology and versions must remain unchanged, Worker bindings must remain identical, and any actual verification failure must automatically restore the preflight operational Worker version.
