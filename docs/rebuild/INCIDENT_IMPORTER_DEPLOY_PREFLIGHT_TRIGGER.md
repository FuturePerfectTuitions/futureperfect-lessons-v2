# Importer production deployment preflight trigger

Read-only gate before deploying the verified English 11+ VR recurrence fix.

No Cloudflare mutation is authorized by this trigger. It records the exact operational Worker rollback version, public Browser/Student topology and current non-secret binding set, and reruns the key importer regressions.
