# Mahu prepared-access production repair trigger

Authorized after the read-only diff diagnostic proved Mahu is the only genuine source-drift snapshot and the dedicated dry run proved the exact authoritative source state: current memberships Y6FE + Y6FM, no lesson/prelesson entitlements, expected current views Year 6 English + Year 6 Maths, and no current views in the stale prepared model. Publish exactly Mahu's prepared-access scope using the canonical compiler/atomic pointer protocol, retain the previous version for rollback, and abort on any concurrent pointer change or source drift.
