# Release visibility production repair trigger

The read-only dry run passed against the exact live state. Authorize only the canonical atomic publication of freshly compiled prepared-access models for the five affected students: Amelie, Connor, Kiaan, Maahi and Zara. The write must abort on any source/pointer drift, preserve the previous pointer as rollback, and automatically restore captured preflight pointers on any publication or verification failure.

No D1, profile, lesson/resource, Worker, route, binding or email mutation is authorized by this trigger.
