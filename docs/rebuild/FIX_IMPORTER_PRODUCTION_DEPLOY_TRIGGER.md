# Importer prepared-access and per-lesson email production deploy

Authorized production deployment after the complete source regression suite passed. The deployment must preserve all live operational Worker bindings, preserve the public Browser -> Student topology and versions, retain the current operational Worker version for rollback, prove zero real-student semantic D1-to-prepared-access mismatches before and after deployment, and automatically roll back on any failed post-deploy gate. Snapshot asOfDate metadata is not an access semantic and may differ without blocking deployment. `mahu1907` is an owner-confirmed test login and is explicitly excluded from the real-student acceptance population. No parent email or synthetic production entitlement may be created by this deployment verification.

The first semantic-parity attempt correctly stopped before deployment because Annisha was the sole real-student semantic mismatch. Run 35089348964 then canonically rebuilt and atomically published Annisha's prepared access from the existing D1 full entitlement for `Y6M2.2`, with previous-version rollback retained and verification PASS. This retrigger may proceed only if the fresh semantic parity gate now reports zero real-student mismatches.
