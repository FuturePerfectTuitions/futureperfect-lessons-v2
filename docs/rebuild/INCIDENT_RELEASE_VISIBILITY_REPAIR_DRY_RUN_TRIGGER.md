# Release visibility repair dry-run trigger

Run the canonical CP11-style access compiler and atomic publisher in strict read-only mode for the exact nine known stale grants across Amelie, Connor, Kiaan, Maahi and Zara. Abort if the mismatch set or production source bindings have changed. No production write is authorized by this trigger.
